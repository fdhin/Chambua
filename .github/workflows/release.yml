name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        # macOS is the primary v1 target; Windows rides along per the spec.
        include:
          - os: macos-latest
            artifact_suffix: macos
          - os: windows-latest
            artifact_suffix: windows-x64

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests
        run: python -m pytest tests/ -q

      - name: Build binary
        run: pyinstaller chambua.spec

      - name: Rename artifact (unix)
        if: runner.os != 'Windows'
        run: |
          VERSION=${GITHUB_REF_NAME#v}
          cp dist/chambua "chambua-${VERSION}-${{ matrix.artifact_suffix }}"
          shasum -a 256 "chambua-${VERSION}-${{ matrix.artifact_suffix }}" > "chambua-${VERSION}-${{ matrix.artifact_suffix }}.sha256"

      - name: Rename artifact (windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $VERSION = $env:GITHUB_REF_NAME.TrimStart('v')
          Copy-Item dist/chambua.exe "chambua-$VERSION-${{ matrix.artifact_suffix }}.exe"
          Get-FileHash "chambua-$VERSION-${{ matrix.artifact_suffix }}.exe" -Algorithm SHA256 | `
            ForEach-Object { "$($_.Hash)  chambua-$VERSION-${{ matrix.artifact_suffix }}.exe" } | `
            Out-File "chambua-$VERSION-${{ matrix.artifact_suffix }}.exe.sha256" -Encoding ascii

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: chambua-${{ matrix.artifact_suffix }}
          path: |
            chambua-*${{ matrix.artifact_suffix }}*
            !chambua-*${{ matrix.artifact_suffix }}*.sha256

      - name: Attach to release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            chambua-*${{ matrix.artifact_suffix }}*
